<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kkllc.admin.domain.material.MaterialMapper">

  <select id="findAll" resultType="Material">
    SELECT id, name, unit, default_price, active FROM material
    <where>
      <if test="activeOnly">AND active = TRUE</if>
    </where>
    ORDER BY name
  </select>

  <select id="findById" resultType="Material">
    SELECT id, name, unit, default_price, active FROM material WHERE id = #{id}
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO material (name, unit, default_price, active)
    VALUES (#{name}, #{unit}, #{defaultPrice}, #{active})
  </insert>

  <update id="update">
    UPDATE material SET name=#{name}, unit=#{unit}, default_price=#{defaultPrice},
      active=#{active}, updated_at=now() WHERE id=#{id}
  </update>
</mapper>
