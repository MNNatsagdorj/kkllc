<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kkllc.admin.domain.quote.QuoteMapper">

  <sql id="cols">
    id, customer_name, phone, product_text, message, estimate,
    status, is_read, source, tg_chat_id, received_at
  </sql>

  <select id="findPage" resultType="Quote">
    SELECT <include refid="cols"/> FROM quote
    <where>
      <if test="status != null and status != ''">AND status = #{status}</if>
    </where>
    ORDER BY received_at DESC, id DESC
    LIMIT #{size} OFFSET #{offset}
  </select>

  <select id="countPage" resultType="long">
    SELECT COUNT(*) FROM quote
    <where>
      <if test="status != null and status != ''">AND status = #{status}</if>
    </where>
  </select>

  <select id="countUnread" resultType="long">
    SELECT COUNT(*) FROM quote WHERE is_read = FALSE
  </select>

  <select id="findById" resultType="Quote">
    SELECT <include refid="cols"/> FROM quote WHERE id = #{id}
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO quote (customer_name, phone, product_text, message, status, is_read, source, tg_chat_id)
    VALUES (#{customerName}, #{phone}, #{productText}, #{message}, #{status}, #{isRead}, #{source}, #{tgChatId})
  </insert>

  <update id="markRead">UPDATE quote SET is_read = TRUE WHERE id = #{id}</update>

  <update id="answer">
    UPDATE quote SET estimate = #{estimate}, status = 'answered', is_read = TRUE WHERE id = #{id}
  </update>

  <update id="updateStatus">UPDATE quote SET status = #{status} WHERE id = #{id}</update>
</mapper>
