<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kkllc.admin.domain.setting.SettingMapper">

  <select id="findAll" resultType="Setting">
    SELECT key, value FROM app_setting ORDER BY key
  </select>

  <select id="findValue" resultType="string">
    SELECT value FROM app_setting WHERE key = #{key}
  </select>

  <update id="upsert">
    INSERT INTO app_setting (key, value, updated_at)
    VALUES (#{key}, #{value}, now())
    ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value, updated_at = now()
  </update>
</mapper>
